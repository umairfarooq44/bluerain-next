/**
 * Created by anser on 5/23/17.
 */

import React from 'react';
import { shallow } from 'enzyme';
import SonosControl from '../../../../src/components/Asset/AssetControl/SonosControl/SonosControl.component';

describe('SonosControl Component Test Specifications', () => {

  it('should find volume slider and slider current value', () => {
    const wrapper = shallow(
      <SonosControl volume={25} />
    );
    const slider = wrapper.find('[label="Volume"]');
    expect(slider.props().value).toBe(25);
  });

  // it('should find control buttons and button current status', () => {
  //   const wrapper = shallow(
  //     <SonosControl play={true} />
  //   );
  //   const slider = wrapper.find('[label="playButton"]');
  //   expect(slider.props().play).toBe(true);
  // });

});
