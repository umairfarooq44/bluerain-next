import React from 'react';

import { Notifications, LoadingBar } from 'bluerain-client-services';

const Layout = props => (

  <div className="layout">
    <Notifications />
    <LoadingBar showFastActions progressIncrease={3} />
    {props.children}
  </div>
);

Layout.propTypes = {
  children: React.PropTypes.object
};

export default Layout;
