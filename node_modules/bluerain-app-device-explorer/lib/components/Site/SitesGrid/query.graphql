query SitesFetchQuery {
    viewer {
        sites(where:{active: true, isDeleted: false}) {
            edges {
                node {
                    id
                    name
                    image
                    siteType {
                        id
                        name
                        icon
                    }
                }
            }
        }
    }
}
