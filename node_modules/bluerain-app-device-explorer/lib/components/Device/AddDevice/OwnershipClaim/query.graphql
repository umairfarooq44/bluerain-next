query AssetFetchQuery ($query: JSON)
{
    viewer {
        me{
            username
            id
        }
    }
    Device {
        DeviceFindOne(filter: $query) {
            id
            name
            meta
        }
    }
}
