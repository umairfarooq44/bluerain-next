query UserInfoQuery ($userId: ID!){
    node (id: $userId) {
        ... on User {
            id
            firstName
            lastName
            email
            profilePicture{
                url
                name
            }
        }
    }
}
