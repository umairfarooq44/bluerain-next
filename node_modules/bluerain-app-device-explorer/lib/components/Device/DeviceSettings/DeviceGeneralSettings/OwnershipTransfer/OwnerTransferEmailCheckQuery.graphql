query EmailCheckQuery ($queryString: JSON!)
{
    viewer {
        me{
            id
        }
        users(query: $queryString) {
            edges{
                node{
                    id
                    email
                }
            }
        }
    }
}
