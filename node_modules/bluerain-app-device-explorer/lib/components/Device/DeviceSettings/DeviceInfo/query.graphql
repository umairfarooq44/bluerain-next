query DeviceInfoQuery ($assetId: ID!){
    node (id: $assetId) {
        ... on Asset {
            id
            state
            meta
            createdAt
            devices {
                edges {
                    node { id name meta }
                }
            }
        }
    }
}
