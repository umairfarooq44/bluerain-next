#query TPLinkTimeChartViewerQuery ($assetId: ID!, $queryString: JSON!)
#{
#    node(id : $assetId) {
#        ... on Asset {
#            id
#            events(
#            where: {type: "command"},
#            # query: $queryString
#            ) {
#                edges {
#                    node {
#                        id
#                        time
#                        data
#                    }
#                }
#            }
#        }
#    }
#}

query TPLinkTimeChartViewerQuery ($queryString:JSON){
    Event{
        EventFind(filter:$queryString, first: 10000){
            edges{
                node{
                    data
                    time
                    id
                }
            }
        }
    }
}
