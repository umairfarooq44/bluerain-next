#query FridgeOpenTimeChartViewerQuery ($assetId: ID!, $queryString: JSON!)
#{
#    node(id : $assetId) {
#        ... on Asset {
#            id
#            events(
#            where: {type: "state"},
#            # query: $queryString
#            ) {
#                edges {
#                    node {
#                        id
#                        time
#                        data
#                    }
#                }
#            }
#        }
#    }
#}
query FridgeOpenTimeChartViewerQuery ($queryString:JSON){
    Event{
        EventFind(filter:$queryString, first: 10000){
            edges{
                node{
                    data
                    time
                    id
                }
            }
        }
    }
}