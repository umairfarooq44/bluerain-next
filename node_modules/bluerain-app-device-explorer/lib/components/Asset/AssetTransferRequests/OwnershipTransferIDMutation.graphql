mutation OwnershipTransferStatusMutation(
$globalId: String,
$name: String!,
$owner: String
){
    assetSave(input: {
    globalId: $globalId,
    name: $name,
    owner:{globalId : $owner}
    }) {
        asset {
            id
        }
    }
}