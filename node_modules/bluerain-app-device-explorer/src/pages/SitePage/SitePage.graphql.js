import { graphql } from 'react-apollo';
import Query from './query.graphql';

export default function gql(Component, siteId) {
  return graphql(Query, {
    options: { variables: { siteId } },
  })(Component);
}
