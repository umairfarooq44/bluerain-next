/**
 * Created by anser on 2/22/17.
 */
import React, { PropTypes } from 'react';
import { Container, Row, Col } from 'reactstrap';
import UserInfoComponent from '../../components/Device/DeviceSettings/DeviceGeneralSettings/OwnershipTransfer/UserInfo.widget';

const propTypes = {
  title: PropTypes.string
};

const defaultProps = {
  title: 'User Info'
};


const OwnershipTransferUserPage = props =>
  // const { assetId } = props.params;
  (
    <div>
      <section>
        <Container>
          <Row>
            <Col lg={{ size: 12 }} className="col-faded">
              <UserInfoComponent />
            </Col>
          </Row>
        </Container>
      </section>
    </div>
  );

OwnershipTransferUserPage.propTypes = propTypes;
OwnershipTransferUserPage.defaultProps = defaultProps;

export default OwnershipTransferUserPage;
