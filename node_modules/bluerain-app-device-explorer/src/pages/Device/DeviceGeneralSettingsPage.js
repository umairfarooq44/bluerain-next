/**
 * Created by anser on 1/16/17.
 */
import React, { PropTypes } from 'react';
import { Container, Row, Col } from 'reactstrap';
import DeviceGeneralSettings from '../../components/Device/DeviceSettings/DeviceGeneralSettings';
import SideBar from '../../components/Device/DeviceSettings/SideBar.widget';

const propTypes = {
  routes: PropTypes.array,
  params: PropTypes.object
};

const defaultProps = {
  title: 'Device General Settings'
};


const DeviceGeneralSettingsPage = (props) => {
  const { assetId } = props.params;
  return (
    <div>
      <section>
        <Container>
          <Row>
            <Col lg={{ size: 3 }}>
              <SideBar assetId={assetId} routes={props.routes} />
            </Col>
            <Col lg={{ size: 9 }} className="col-faded">
              <DeviceGeneralSettings assetId={assetId} />
            </Col>
          </Row>
        </Container>
      </section>
    </div>
  );
};

DeviceGeneralSettingsPage.propTypes = propTypes;
DeviceGeneralSettingsPage.defaultProps = defaultProps;

export default DeviceGeneralSettingsPage;
