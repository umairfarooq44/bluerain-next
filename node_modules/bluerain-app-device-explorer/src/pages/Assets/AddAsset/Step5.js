import React, { PropTypes } from 'react';
import 'bluerain-bootstrap-theme/dist/css/components/data-list.css';
import OwnershipClaim from '../../../components/Device/AddDevice/OwnershipClaim/OwnershipClaim.widget';


const propTypes = {
  getStore: PropTypes.func

};

// this step is about entering the device name into the data base.
// we are sending the macid of device and site ID

export default class Step5 extends React.Component {

  constructor(props) {
    super(props);

    this.state = {
      stateFlag: 0,
    };
  }


  render() {
    return (

      <div className="step Step5">
        <OwnershipClaim macId={this.props.getStore().macId} siteId={this.props.getStore().siteid} />
      </div>
    );
  }

}

Step5.propTypes = propTypes;
