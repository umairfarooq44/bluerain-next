query AssetPageQuery ($assetId: ID!){
    node (id: $assetId) {
        ... on Asset {
            id
            name
            state
            site { id name image }
            assetType { id name icon }
        }
    }
}
