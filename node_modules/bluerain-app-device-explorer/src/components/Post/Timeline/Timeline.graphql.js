import { graphql } from 'react-apollo';
import Query from './query.graphql';

export default function gql(component) {
  return graphql(Query, {
    options: ({ assetId, queryString }) => ({ variables: { assetId, queryString } })
  })(component);
}
