query SiteEditQuery ($siteId: ID!){
    node (id: $siteId) {
        ... on Site {
            id
            name
            assets{
                edges{
                    node{
                        id
                        name
                    }
                }
            }
        }
    }
}

