/**
 * Created by anser on 1/3/17.
 */
import React from 'react';
import { storiesOf, action } from '@kadira/storybook';
import { withKnobs, text, object } from '@kadira/storybook-addon-knobs';
import SiteForm from './SiteForm.component';

const stories = storiesOf('New Site Form', module);
stories.addDecorator(withKnobs);
stories.add('Full Form Component', () => (
  <SiteForm />));

stories.add('Without Location', () => (
  <SiteForm
    mode={text('Mode', 'room')}
    onModeChange={action('Mode Change')}
    modeOptions={object('Mode Options', [
      {
        label: 'Room',
        value: 'room'
      },
      {
        label: 'House',
        value: 'house'
      },
      {
        label: 'Office',
        value: 'office'
      },
      {
        label: 'Kitchen',
        value: 'kitchen'
      }
    ])}
  />));

stories.add('Without Image', () => (
  <SiteForm
    mode={text('Mode', 'room')}
    onModeChange={action('Mode Change')}
    modeOptions={object('Mode Options', [
      {
        label: 'Room',
        value: 'room'
      },
      {
        label: 'House',
        value: 'house'
      },
      {
        label: 'Office',
        value: 'office'
      },
      {
        label: 'Kitchen',
        value: 'kitchen'
      }
    ])}
  />));
