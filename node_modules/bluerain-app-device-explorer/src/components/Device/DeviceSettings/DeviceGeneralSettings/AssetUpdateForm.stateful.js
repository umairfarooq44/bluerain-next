import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import AssetUpdateForm from './AssetUpdateForm';

const params = {
  dataField: 'node'
};

export default stateful(AssetUpdateForm, params);
