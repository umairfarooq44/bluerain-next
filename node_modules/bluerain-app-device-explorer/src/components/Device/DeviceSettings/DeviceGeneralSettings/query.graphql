query DeviceInfoQuery ($assetId: ID!){
    node (id: $assetId) {
        ... on Asset {
            id
            name
            state
            meta
            site { id name image }
            createdAt
        }
    }
}
