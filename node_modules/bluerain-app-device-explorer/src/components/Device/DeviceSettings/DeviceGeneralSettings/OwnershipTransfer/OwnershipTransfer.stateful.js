import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import OwnershipTransfer from './OwnershipTransfer.component';

const params = {
  dataField: 'node'
};

export default stateful(OwnershipTransfer, params);
