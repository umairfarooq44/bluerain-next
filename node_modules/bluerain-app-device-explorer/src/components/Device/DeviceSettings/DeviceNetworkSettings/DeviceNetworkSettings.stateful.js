import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import DeviceInfo from './DeviceNetworkSettings.component';

const params = {
  dataField: 'node'
};

export default stateful(DeviceInfo, params);
