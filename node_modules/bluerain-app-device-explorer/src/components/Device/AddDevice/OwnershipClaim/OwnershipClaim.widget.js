import React, { PropTypes } from 'react';
import Widget from 'bluerain-ui/lib/Widget/widget';
import WidgetHeader from 'bluerain-ui/lib/Widget/widgetHeader';
import WidgetTitle from 'bluerain-ui/lib/Widget/widgetTitle';
import WidgetContent from 'bluerain-ui/lib/Widget/widgetContent';
import OwnershipClaim from './OwnershipClaim.component';

const propTypes = {
  macId: PropTypes.string,
  siteId: PropTypes.string
};

export default class SiteFormWidget extends React.Component {

  render() {
    // console.log('props ', this.props)

    return (
      <Widget>
        <WidgetHeader>
          <WidgetTitle>Give a nick name to your appliance</WidgetTitle>
        </WidgetHeader>
        <WidgetContent>
          <OwnershipClaim macId={this.props.macId} siteId={this.props.siteId} wifiName="Test WifiName" />
        </WidgetContent>
      </Widget>
    );
  }
}
SiteFormWidget.propTypes = propTypes;
