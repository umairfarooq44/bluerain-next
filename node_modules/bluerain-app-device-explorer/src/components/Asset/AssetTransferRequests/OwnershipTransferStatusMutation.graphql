mutation OwnershipTransferStatusMutation(
$globalId: String,
$status: String
){
    transferRequestSave(input: {
    globalId: $globalId,
    status: $status
    }) {
        transferRequest {
            id status
            createdAt updatedAt
        }
    }
}