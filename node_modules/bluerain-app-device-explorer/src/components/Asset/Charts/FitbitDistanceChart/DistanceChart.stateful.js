import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import DistanceChartContainer from './DistanceChart.container';

const params = {
  dataField: 'node'
};

export default stateful(DistanceChartContainer, params);
