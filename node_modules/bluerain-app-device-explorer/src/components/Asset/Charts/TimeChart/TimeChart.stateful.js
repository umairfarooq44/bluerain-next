import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import TimeChartContainer from './TimeChart.container';
const params = {
  dataField: 'node'
};

export default stateful(TimeChartContainer, params);
