#query TimeChartViewerQuery ($assetId: ID!, $queryString:JSON)
#{
#    node(id : $assetId) {
#        ... on Asset {
#            id
#            events(
#            where: $queryString,
#            order :"time DESC",
#            ) {
#                edges {
#                    node {
#                        id
#                        time
#                        data
#                    }
#                }
#            }
#        }
#    }
#}


query TimeChartViewerQuery ($queryString:JSON){
    Event{
        EventFind(filter:$queryString, first: 100){
            edges{
                node{
                    data
                    time
                    id
                }
            }
        }
    }
}
