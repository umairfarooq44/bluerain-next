import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import FridgeOpenTimeChartContainer from './FridgeOpenTimeChart.container';
const params = {
  dataField: 'node'
};

export default stateful(FridgeOpenTimeChartContainer, params);
