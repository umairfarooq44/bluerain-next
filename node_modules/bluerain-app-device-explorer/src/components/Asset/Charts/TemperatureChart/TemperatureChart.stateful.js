import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import TemperatureChartContainer from './TemperatureChart.container';

const params = {
  dataField: 'node'
};

export default stateful(TemperatureChartContainer, params);
