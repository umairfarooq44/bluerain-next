import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import BillChartContainer from './BillChart.container';

const params = {
  dataField: 'node'
};

export default stateful(BillChartContainer, params);
