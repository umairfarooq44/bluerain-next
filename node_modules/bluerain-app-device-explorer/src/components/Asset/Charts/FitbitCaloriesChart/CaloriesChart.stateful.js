import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import CaloriesChartContainer from './CaloriesChart.container';

const params = {
  dataField: 'node'
};

export default stateful(CaloriesChartContainer, params);
