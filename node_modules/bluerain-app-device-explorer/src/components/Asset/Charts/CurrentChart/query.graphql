#query CurrentChartViewerQuery ($assetId: ID!, $queryString: JSON!)
#{
#    node(id : $assetId) {
#        ... on Asset {
#            id
#            events(
#            where: $queryString,
#            order :"time DESC",
#            first: 100,
#            # query: $queryString
#            )
#            {
#                edges {
#                    node {
#                        id
#                        time
#                        data
#                    }
#                }
#            }
#        }
#    }
#}

query EnergyChartViewerQuery ($queryString:JSON){
    Event{
        EventFind(filter:$queryString, first: 100){
            edges{
                node{
                    data
                    time
                    id
                }
            }
        }
    }
}