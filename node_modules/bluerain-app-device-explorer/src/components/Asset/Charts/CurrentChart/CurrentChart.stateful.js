import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import CurrentChartContainer from './CurrentChart.container';
const params = {
  dataField: 'node'
};

export default stateful(CurrentChartContainer, params);
