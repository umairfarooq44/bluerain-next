import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import VoltageChartContainer from './VoltageChart.container';
const params = {
  dataField: 'node'
};

export default stateful(VoltageChartContainer, params);
