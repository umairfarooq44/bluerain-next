import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import EnergySavingChartContainer from './EnergySavingChart.container';
const params = {
  dataField: 'node'
};

export default stateful(EnergySavingChartContainer, params);
