#query TemperatureChartViewerQuery ($assetId: ID!, $queryString: JSON!)
#{
#    node(id : $assetId) {
#        ... on Asset {
#            id
#            events(
#            where: {type: "state"},
#            first: 100,
#            # query: $queryString
#            ) {
#                edges {
#                    node {
#                        id
#                        time
#                        data
#                    }
#                }
#            }
#        }
#    }
#}
query CompressorTemperatureChartViewerQuery ($queryString:JSON){
    Event{
        EventFind(filter:$queryString, first: 100){
            edges{
                node{
                    data
                    time
                    id
                }
            }
        }
    }
}