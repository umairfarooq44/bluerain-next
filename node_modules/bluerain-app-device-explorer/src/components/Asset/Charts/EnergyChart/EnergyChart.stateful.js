import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import EnergyChartContainer from './EnergyChart.container';
const params = {
  dataField: 'node'
};

export default stateful(EnergyChartContainer, params);
