import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import PowerChartContainer from './PowerChart.container';
const params = {
  dataField: 'node'
};

export default stateful(PowerChartContainer, params);
