import { stateful } from 'bluerain-ui/lib/StatefulComponent';
import AssetLocationComponent from './AssetLocation.component';

export default stateful(AssetLocationComponent, { dataField: 'viewer' });
