/**
 * Created by anser on 3/7/16.
 */
import React, { PropTypes } from 'react';
import Widget from 'bluerain-ui/lib/Widget/widget';
import WidgetHeader from 'bluerain-ui/lib/Widget/widgetHeader';
import WidgetTitle from 'bluerain-ui/lib/Widget/widgetTitle';
import WidgetContent from 'bluerain-ui/lib/Widget/widgetContent';
import RefrigeratorSensorInfo from './RefrigeratorSensorInfo.container';

const propTypes = {
  data: PropTypes.object,
};

const defaultProps = {};
export default class RefrigeratorSensorInfoWidget extends React.Component {

  render() {
    return (
      <Widget>
        <WidgetHeader>
          <WidgetTitle>Sensors</WidgetTitle>
        </WidgetHeader>
        <WidgetContent>
          <RefrigeratorSensorInfo data={this.props.data} />
        </WidgetContent>
      </Widget>
    );
  }
}

RefrigeratorSensorInfoWidget.propTypes = propTypes;
RefrigeratorSensorInfoWidget.defaultProps = defaultProps;
