/**
 * Created by anser on 12/27/16.
 */
import React from 'react';
import Widget from 'bluerain-ui/lib/Widget/widget';
import WidgetHeader from 'bluerain-ui/lib/Widget/widgetHeader';
import WidgetTitle from 'bluerain-ui/lib/Widget/widgetTitle';
import WidgetContent from 'bluerain-ui/lib/Widget/widgetContent';
import LightControl from './LightControl.container';

const propTypes = {};

const defaultProps = {};

export default class LightControlWidget extends React.Component {
  render() {
    return (
      <Widget>
        <WidgetHeader>
          <WidgetTitle>Control</WidgetTitle>
        </WidgetHeader>
        <WidgetContent>
          <LightControl {...this.props} />
        </WidgetContent>
      </Widget>
    );
  }
}

LightControlWidget.propTypes = propTypes;
LightControlWidget.defaultProps = defaultProps;
