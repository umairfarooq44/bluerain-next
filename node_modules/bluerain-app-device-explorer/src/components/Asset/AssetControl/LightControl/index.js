import { graphql } from 'react-apollo';
import query from './query.graphql';
import mutation from './mutation.graphql';
import LightControlWidget from './LightControl.widget';

const componentWithQuery = graphql(query, {
  options: ({ assetId }) => ({ variables: { assetId } })
})(LightControlWidget);

export default graphql(mutation)(componentWithQuery);
