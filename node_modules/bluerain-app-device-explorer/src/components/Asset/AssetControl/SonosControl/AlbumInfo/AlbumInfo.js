/**
 * Created by anser on 12/22/16.
 */
import React from 'react';
import ListView from 'bluerain-ui/lib/ListView/ListView';
import ListViewItem from 'bluerain-ui/lib/ListView/ListViewItem';
import ListViewItemLabel from 'bluerain-ui/lib/ListView/ListViewItemLabel';
import ListViewItemImage from 'bluerain-ui/lib/ListView/ListViewItemImage';
import Image from 'bluerain-ui/lib/ListView/Image';


// const propTypes = {
//   albumInfo: PropTypes.array,
// };
//
// const defaultProps = {
//   albumInfo: []
// };

const AlbumInfoContainer = (props) => {
  // const {
  //   // albumInfo
  // } = props;
  const image = 'https://s3-us-west-2.amazonaws.com/bluerainimages/air-conditioner.svg';
  const trackTitle = 'Hum hain pakistani hum to ';
  const albumName = 'Unknown';
  const singerName = 'Junaid Jamshed';

   /* if (item.trackTitle) {*/

   /* trackTitle = item.trackTitle;*/

   /* }*/

  // if (item.albumName) {
  //   albumName = item.albumName;
  // }
  // if (item.singerName) {
  //   singerName = item.singerName;
  // }
  return (
    <ListView>
      <ListViewItem >
        <ListViewItemImage>
          <Image src={image} />
        </ListViewItemImage>
        <div>
          <ListViewItemLabel>
            {trackTitle}
          </ListViewItemLabel>
          <ListViewItemLabel>
            {albumName}
          </ListViewItemLabel>
          <ListViewItemLabel tag="strong">
            {singerName}
          </ListViewItemLabel>
        </div>
      </ListViewItem>
    </ListView>
    /* <ListView>
     {
     albumInfo.map((item, i) => {

     let image = 'https://s3-us-west-2.amazonaws.com/bluerainimages/air-conditioner.svg'
     let { trackTitle, albumName, singerName } = '';
     {/!*if (item.imageSRC) {*!/}
     {/!*image = (*!/}
     {/!*<ListViewItemImage>*!/}
     {/!*<Image src='' />*!/}
     {/!*</ListViewItemImage>*!/}
     {/!*);*!/}
     {/!*}*!/}
     if (item.trackTitle) {
     trackTitle = item.trackTitle;
     }
     if (item.albumName) {
     albumName = item.albumName;
     }
     if (item.singerName) {
     singerName = item.singerName;
     }
     return (
     <ListViewItem key={i}>
     <ListViewItemImage>
     <Image src={image} />
     </ListViewItemImage>
     <ListViewItemLabel>
     {trackTitle}
     </ListViewItemLabel>
     <ListViewItemLabel>
     {albumName}
     </ListViewItemLabel>
     <ListViewItemLabel>
     {singerName}
     </ListViewItemLabel>
     </ListViewItem>
     );
     })
     }
     </ListView>*/
  );
};

// AlbumInfoContainer.propTypes = propTypes;
// AlbumInfoContainer.defaultProps = defaultProps;

export default AlbumInfoContainer;
