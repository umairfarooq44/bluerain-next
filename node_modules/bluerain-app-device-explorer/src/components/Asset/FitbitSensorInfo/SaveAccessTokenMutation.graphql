mutation DeviceUpdate ($globalId: String!, $deviceName: String!, $meta:JSON!){
    deviceSave(input: {
    globalId: $globalId,
    name:$deviceName,
    meta:$meta,
    }) {
        device {
            id name meta
            createdAt updatedAt
        }
    }
}