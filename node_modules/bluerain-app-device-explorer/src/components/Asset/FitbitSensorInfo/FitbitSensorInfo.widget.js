/**
 * Created by anser on 12/27/16.
 */
import React, { PropTypes } from 'react';
import Widget from 'bluerain-ui/lib/Widget/widget';
import WidgetHeader from 'bluerain-ui/lib/Widget/widgetHeader';
import WidgetTitle from 'bluerain-ui/lib/Widget/widgetTitle';
import WidgetContent from 'bluerain-ui/lib/Widget/widgetContent';
import FitbitSensorInfo from './FitbitSensorInfo.container';

const propTypes = {
  data: PropTypes.object,
};

const defaultProps = {};
export default class FitbitSensorInfoWidget extends React.Component {

  render() {
    return (
      <Widget>
        <WidgetHeader>
          <WidgetTitle>Sensors</WidgetTitle>
        </WidgetHeader>
        <WidgetContent>
          <FitbitSensorInfo data={this.props.data} />
        </WidgetContent>
      </Widget>
    );
  }
}

FitbitSensorInfoWidget.propTypes = propTypes;
FitbitSensorInfoWidget.defaultProps = defaultProps;